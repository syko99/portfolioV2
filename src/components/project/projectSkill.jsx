const ProjectSkill = ({icon}) => {
    return (
        <div className='w-7'>
            <img src={icon.image} alt={icon.alt} width={100} height={100} />
        </div>
    )
}

export default ProjectSkill
